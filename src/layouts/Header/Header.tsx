import React from 'react';
import Filters from '../../components/Filters/Filters';

const Header = () => {
	return (
		<header>
			<h1>React Shop</h1>
			<Filters />
		</header>
	);
};

export default Header;
